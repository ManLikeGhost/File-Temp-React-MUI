git add . && git commit -m "Added Avatar to Account setting Sidebar menu" && git push origin master

Fetch PR, test before merging
https://gist.github.com/adam-p/15413fabef6cffecd897

For the carosel
https://www.npmjs.com/package/material-auto-rotating-carousel
https://codesandbox.io/s/mystifying-varahamihira-dphsr?fontsize=14&file=/src/index.js


https://github.com/Learus/react-material-ui-carousel
https://learus.github.io/react-material-ui-carousel/


https://levelup.gitconnected.com/adding-transitions-to-a-react-carousel-with-material-ui-b95825653c1b


axios.get('https://api.github.com/user', {
  headers: {
    'Authorization': `Bearer ${access_token}`
  }
})
.then((res) => {
  console.log(res.data)
})
.catch((error) => {
  console.error(error)
})

const token = Buffer.from(`${username}:${password}`, 'utf8').toString('base64')

const url = 'https://...'
const data = {
  ...
}

const res = await axios.get('https://httpbin.org/get', {
  headers: {
    'Authorization': 'my secret token'
  }
});




axios.post(url, data, {
  headers: {
    'Authorization': `Bearer ${tokenStr}`
  },
})

const body = {};
const res = await axios.post('https://httpbin.org/post', body, {
  headers: {
    'Authorization': 'my secret token'
  }
});


send props via link
component A

<Link to={{ pathname: '/new-upgrade', subscription: subscription }}>
               <Button className={classes.button}>
                  PROCEED
               </Button>


    Component B

    import {useLocation} from "react-router-dom"; 

    let data = useLocation();
// console.log(data.subscription)          